Linux Navigation & Files
Learnt:
The shell prompt-- username@hostname:current_directory$ 
echo - echoes back the argument to the terminal 
pwd – prints the current working directory
cd - move around the file system 
Absolute Path: This is the full path starting from the root directory (/). 
Relative Path: This path is relative to your current location in the filesystem.
some shortcuts:   
cd .(current directory)        cd ..(parent directory)        cd ~(home directory)        cd -(previous directory)
ls - list the directories and files in your current directory.
ls -a --> hidden files         ls -l --> long format               ls -r --> reverse order
mkdir --> create directories 
mkdir -p --> create nested directories 
rm --> remove a file 
rm -f file1( forecful deletion)    rm -i file (interactive deletion)     rm -r directory(removes dierectories)     rmdir directory(removes empty dierectories)
