Day 6 – Linux Fundamentals & Cyber Relevance

Revised core Linux commands.
Learned file permissions and chmod.
Practiced wildcards (* ? []).
Used grep and find for searching files.
Permissions are always User–Group–Others, and r=4, w=2, x=1
Understood why Linux is important for cybersecurity and investigations.
LABEX 
uid-->Your User ID (a unique numerical identifier).
gid-->Your primary Group ID.
groups-->All the groups you are a member of.
htop=It's like a dashboard that gives you a real-time view of what's happening inside your computer.
we'll use sudo apt install to install htop:
sudo: Stands for "SuperUser DO." It allows you to execute commands with administrator privileges (temporarily).
apt: The tool for installing and managing software.
install: Tells apt that we want to install a program.
htop: The specific program we want to install.
echo command does two things:
echo is a command that prints text.
The > symbol redirects the output of echo into a file named file2.txt. If the file doesn't exist, it's created. If it does exist, its content is replaced.
